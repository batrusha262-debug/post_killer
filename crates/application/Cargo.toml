[package]
name = "post-killer-application"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
post-killer-domain = { path = "../domain" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
